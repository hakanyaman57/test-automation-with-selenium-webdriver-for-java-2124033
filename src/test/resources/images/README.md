# SikuliX template images

Place your OS-specific template images here:

- `file_name_input.png`
- `open_button.png`

Capture templates from the same machine/theme/resolution where the test will run.

These images are only needed for `SikuliXNativeFileUploadTest`.
The quick-start demo in `examples.SikuliXQuickDemo` generates its own matchable visuals at runtime.
